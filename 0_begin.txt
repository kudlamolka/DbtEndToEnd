
********** Project Description **********

This Project is about learning to use DBT with Snowflake.

********** 25 July 2026 **********

Downloaded the data from the below URL 
https://grouplens.org/datasets/movielens/20m/
for this project.

Created a S3 bucket called 'dbt-e2e-kudla-molka', where I uploaded the csv files which I downloaded from the above URL.

It had 6 CSV Files:
"tags.csv"
"genome-scores.csv"
"genome-tags.csv"
"links.csv"
"movies.csv"
"ratings.csv" 

Then we created the Setup in snowflake

We first created a suitable role (TRANSFORM), which had all the permissions that of the ACCOUNTADMIN

Then a User was created named 'dbt'. 
This role was given default warehouse, database and role.

The proper permissions were also given accordingly. Refere the 00_Create_Database_Objects_and_Roles.sql

--------------------------------------------------------------------------------------------------------------

This will be followed by creation of Stage. 
For the stage, we will need the IAM User. So, first we created an IAM User on AWS.

This user was named 'snowflakenetflixuser'
and given AmazonS3FullAccess permission.

We created a new Access Key for this user.

This was added in CREDENTIALS part of the create Stage directly in Snowflake.

This is shown in the 01_S3_Stage_Connection.sql

----------------------------------------------------------------------------------------------------------------

Then we proceed with creating the tables for the data from the CSV files
which are there in the Stage which is the S3 bucket

The tables are named as raw_<csv_file_name>

----------------------------------------------------------------------------------------------------------------

*** DBT ***

In DBT there are 2 types DBT Cloud (Paid - UI)) and DBT Core (Open source - CLI)

We are using the Core, so there we need to do some setup.

First create a Python virtual environment. 

%
python -m venv dbte2e
dbte2e/Scripts/activate
%

Then we move on to install the required packages

%
pip install dbt-snowflake==1.9.0
%

We need to create a root folder for dbt (.dbt), so the below commands are run

%
mkdir ~/.dbt
%

Then we have to create a DBT Project folder using the dbt init <project name>
%
dbt init netflix
%

Since I am using Snowflake, it first asked for the database, and I said I want the snowflake,
Then it asked details related to Snowflake account
1) Account identifier
2) Username
3) Password
4) Role
5) Warehouse
6) Database
7) Schema
8) Thread

All these are same details that was used, when we created the dbt user in snowflake.

With this, the profiles.yml would be created in the .dbt folder

